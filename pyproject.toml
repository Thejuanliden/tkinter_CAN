[project]
name = "tkinter-can"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "cantools>=41.3.0",
    "python-can>=4.6.1",
]
